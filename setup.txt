To run the application locally open 3 terminals:
- cd frontend; npm install; npm start
- cd backend; pip install -r requirements.txt; python app.py
- cd alerting_service; pip install -r requirements.txt; python app.py
Access http://localhost:4200 for frontend. 